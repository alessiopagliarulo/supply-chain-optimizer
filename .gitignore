# Environment
.env
.env.local
.env.*.local

# Dependencies
node_modules/
__pycache__/
*.pyc
venv/
.venv/

# Build
dist/
build/
*.egg-info/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*

# Database
*.db
*.db-journal
*.db-wal
*.db-shm
*.sqlite
migrations/versions/
# ...but ship the seeded SQLite snapshot so Render has real data on deploy
!backend/supply_chain.db

# ML Models
data/models/
backend/data/ml_models/*.joblib
# ...but ship the small trained models so ML endpoints work in prod.
# regime.joblib / regime_features.joblib are tracked too: without them the
# deployed instance had no regime model at all and app/ml/serving.py fell back
# to replaying a frozen current_stress_prob out of metrics.joblib as if it were
# live model output. Ship the model or report the signal as unavailable —
# never both-and-pretend.
!backend/data/ml_models/lead_time.joblib
!backend/data/ml_models/feature_cols.joblib
!backend/data/ml_models/metrics.joblib
!backend/data/ml_models/regime.joblib
!backend/data/ml_models/regime_features.joblib

# MLflow local tracking store + artifacts (P5)
backend/mlruns/
mlruns/
mlartifacts/

# Playwright MCP artifacts
.playwright-mcp/

# Screenshots / test artifacts
test-screenshots/
/*.png
/Screenshot*.png

# Claude Code local state (personal) — agent memory, agent definitions, settings.
# The whole directory is local-only: it holds machine-specific paths and assistant
# configuration that is not part of the application. Untracked 2026-09-07; the files
# remain on the maintainer's disk.
.claude/
.mcp.json

# Personal research notes
backend/apimcpresearch.txt

# Superpowers brainstorming visual companion
.superpowers/

# UI gate run artifacts (frontend/scripts/ui-gate.cjs writes beside itself)
frontend/scripts/gate-shots/
frontend/scripts/gate-report.json

# ── Local-only working notes and developer conveniences (2026-09-07) ──────────
# These stay on the maintainer's disk but are not part of the published project.
# They are working notes for the assistant-driven development process, not
# documentation of the system itself.
CLAUDE.md
LEARNINGS.md
LOOP-DASHBOARD.md
launch
graphify-out/

# Session handoff notes and superseded point-in-time audits (working notes).
docs/handoffs/
docs/archive/handoffs/
docs/archive/history/
docs/archive/SESSION-PROCESS-ERRORS-2026-08-26.md

# Personal teaching workspace (see docs/learning/MISSION.md) — deliberately not published.
docs/learning/
